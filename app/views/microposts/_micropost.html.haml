%tr
  %td
    %span.content= wrap micropost.content
    %span.timestamp Posted #{time_ago_in_words micropost.created_at} ago.
  -# workaround for weird rails bug, listing 11.38
  -# note that workaround in book doesn't work in Haml,
  -# hitting database again for each user unfortunately
  - cu = User.find(micropost.user_id)
  - if current_user == cu
    %td= link_to "delete", micropost, :method => :delete,
      :confirm => "Are you sure?", :title => micropost.content