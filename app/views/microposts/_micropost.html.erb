<tr>
  <td class="micropost">
    <span class="content"><%= wrap micropost.content %></span>
    <span class="timestamp">
      Posted <%= time_ago_in_words micropost.created_at %> ago.
    </span>
  </td>
  <% # workaround for weird rails bug, listing 11.38 %>
  <% cu = micropost.user rescue User.find(micropost.user_id) %>
  <% if current_user == cu %>
  <td>
    <%= link_to "delete", micropost, :method => :delete,
          :confirm => "Are you sure?", :title => micropost.content %>
  </td>
  <% end %>
</tr>