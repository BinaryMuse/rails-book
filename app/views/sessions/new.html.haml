%h1 Sign in
= form_for(:session, :url => sessions_path) do |f|
  .field
    = f.label :email
    <br />
    = f.text_field :email, :class => 'title', :value => (params[:session].nil?) ? nil : params[:session][:email]
  .field
    = f.label :password
    <br />
    = f.password_field :password, :class => 'title'
  .actions
    = f.submit "Sign in"
%p
  New user?
  = link_to "Sign up now!", signup_path